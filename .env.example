# SEC API Configuration
# REQUIRED: Set your email address for the SEC API User-Agent
USER_AGENT=SECurityTr8Ker/1.0 (your.email@address.com)

# SEC Rate Limiting Configuration 
# Based on SEC's limit of 10 requests per second
SEC_REQUEST_INTERVAL=0.2   # Time to wait between requests (allows 5 req/second, half of SEC's limit)
SEC_MAX_CONCURRENT_REQUESTS=3   # Maximum concurrent requests (conservative but efficient)
SEC_MAX_RETRIES=5   # Maximum number of retries for failed requests

# OPTIONAL Notification Modules
# Configure only the notification methods you want to use
# Leave others commented out to disable them

# Microsoft Teams Configuration
TEAMS_WEBHOOK_URL=your_teams_webhook_url

# Telegram Configuration
TELEGRAM_BOT_TOKEN=your_tg_bot_token
TELEGRAM_CHAT_ID=your_tg_username

# Twitter Configuration
TWITTER_API_KEY=your_twitter_api_key
TWITTER_API_SECRET=your_twitter_api_secret
TWITTER_BEARER_TOKEN=your_twitter_api_bearer_token
TWITTER_ACCESS_TOKEN=your_twitter_api_access_token
TWITTER_ACCESS_TOKEN_SECRET=your_twitter_api_token_secret

# Slack Configuration
SLACK_WEBHOOK_URL=your_slack_webhook_url
